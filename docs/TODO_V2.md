# TODO_V2

`FEATURE_SPEC_V2.md` 기준 확장 기능 구현 체크리스트입니다.  
현재 v0.1 완료 상태를 기준으로 v0.2 ~ v0.3 범위를 단계적으로 반영합니다.

## 1. 공통 준비

- [x] `src/types/quiz.ts`에 `AnswerMode` 타입 추가
- [x] `WrongAnswer.userAnswer`를 `number | null`로 확장
- [x] `WrongAnswer.reason`에 `wrong-answer | timeout` 추가
- [x] `StudyRecord` 타입 정의
- [x] `src/app/page.tsx`에 신규 상태 추가 뼈대 반영
- [x] 공통 초기화 함수 범위 점검
- [x] 세션 저장 중복 방지용 플래그 설계

## 2. Phase 1

### 2.1 효과음 on/off

- [x] `soundEnabled` 상태 추가
- [x] 정답/오답 효과음 실행 전에 `soundEnabled` 조건 적용
- [x] 시작 화면에 효과음 토글 UI 추가
- [x] 퀴즈 화면에 효과음 토글 UI 추가
- [x] 토글 변경 시 즉시 반영 확인

### 2.2 연속 정답 보상

- [x] `streakCount` 상태 추가
- [x] `bestStreak` 상태 추가
- [x] 정답 시 streak 증가 로직 반영
- [x] 오답 시 streak 초기화 로직 반영
- [x] 시간 초과 시 streak 초기화 처리 설계
- [x] 퀴즈 화면에 연속 정답 보상 문구 표시
- [x] 특정 streak 기준 문구 정책 적용

### 2.3 결과 화면 시각 강화

- [x] 결과 헤더 시각 강화
- [x] 정답/오답/정답률 카드 강조 개선
- [x] `bestStreak` 표시 추가
- [x] 현재 학습 모드 표시 추가
- [x] 타이머 모드 사용 여부 표시 추가
- [x] 시간 초과 수 표시 영역 설계
- [x] 오답 다시 풀기 버튼 우선순위 유지 확인

## 3. Phase 2

### 3.1 직접 입력 모드

- [x] `answerMode` 상태 추가
- [x] `inputAnswer` 상태 추가
- [x] 시작 화면에 풀이 방식 선택 UI 추가
- [x] 퀴즈 화면에 입력 모드 분기 UI 추가
- [x] `input type="number"` 기반 입력창 추가
- [x] 확인 버튼 추가
- [x] 빈 입력값 제출 방지
- [x] 음수 입력 방지
- [x] 채점 후 입력창 비활성화
- [x] 다음 문제 이동 시 `inputAnswer` 초기화
- [x] 입력값도 기존 채점 함수로 연결

### 3.2 타이머 모드

- [x] `isTimerMode` 상태 추가
- [x] `timePerQuestion` 상태 추가
- [x] `timeLeft` 상태 추가
- [x] 시작 화면에 타이머 모드 토글 추가
- [x] 문제당 제한 시간 선택 UI 추가
- [x] 문제 시작 시 타이머 초기화
- [x] 1초 카운트다운 로직 구현
- [x] 채점 완료 시 타이머 중지
- [x] 시간 초과 시 자동 오답 처리
- [x] 시간 초과 시 `reason: "timeout"` 저장
- [x] 시간 초과 시 `userAnswer: null` 저장
- [x] 다음 문제는 수동 이동 유지
- [x] 화면 전환 및 언마운트 시 timer cleanup 확인
- [x] 퀴즈 화면 상단 시간 표시 UI 추가
- [x] 시간이 적을 때 강조 스타일 추가

## 4. Phase 3

### 4.1 학습 기록 저장

- [x] `src/lib/studyRecords.ts` 추가
- [x] `loadStudyRecords()` 구현
- [x] `saveStudyRecord()` 구현
- [x] `clearStudyRecords()` 필요 여부 결정
- [x] localStorage key를 `gugu-study-records`로 적용
- [x] JSON 파싱 실패 fallback 처리
- [x] 저장 실패 try/catch 처리
- [x] 결과 화면 진입 시 1회 저장 로직 반영
- [x] `sessionSaved`로 중복 저장 방지
- [x] 최신 기록이 앞에 오도록 정렬
- [x] 최근 기록 개수 제한 정책 결정
- [x] 결과 화면 하단에 최근 기록 3~5개 노출

## 5. 컴포넌트별 반영 포인트

### 5.1 `src/app/page.tsx`

- [x] 세션 상태 확장
- [x] 공통 reset 함수 정리
- [x] 채점 로직 단일 진입점 유지
- [x] 타이머 effect 및 cleanup 구현
- [x] 결과 저장 트리거 구현

### 5.2 `src/components/StartScreen.tsx`

- [x] 답안 모드 선택 UI 추가
- [x] 타이머 모드 설정 UI 추가
- [x] 효과음 토글 UI 추가
- [x] 기존 단 선택 흐름과 충돌 없는지 확인

### 5.3 `src/components/QuizScreen.tsx`

- [x] 객관식/입력 모드 분기 렌더링
- [x] 타이머 표시 추가
- [x] streak 보상 UI 추가
- [x] 효과음 토글 UI 추가
- [ ] 입력 모드 접근성 점검

### 5.4 `src/components/ResultScreen.tsx`

- [x] 결과 요약 카드 재구성
- [x] 최고 streak 표시
- [x] 시간 초과 수 표시
- [x] 학습 모드 표시
- [x] 최근 기록 표시

### 5.5 유틸 및 타입

- [x] 효과음 유틸 호출 구조 정리
- [x] 기록 저장 유틸 추가
- [x] 타입 확장에 따른 영향 범위 점검

## 6. 수동 테스트

### 6.1 효과음 on/off

- [ ] on 상태에서 정답 효과음 재생 확인
- [ ] on 상태에서 오답 효과음 재생 확인
- [ ] off 상태에서 모든 효과음 비활성화 확인
- [ ] 토글 직후 바로 반영되는지 확인

### 6.2 연속 정답 보상

- [ ] 2연속 정답 시 보상 문구 확인
- [ ] 3연속 정답 시 보상 문구 확인
- [ ] 오답 시 streak 초기화 확인
- [ ] 최고 streak 기록 정확성 확인

### 6.3 직접 입력 모드

- [ ] 입력 모드 선택 후 시작 가능
- [ ] 빈 값 제출 시 채점되지 않음
- [ ] 숫자 입력 후 확인 버튼으로 채점 가능
- [ ] 채점 후 입력창 비활성화 확인
- [ ] 다음 문제 이동 시 입력값 초기화 확인

### 6.4 타이머 모드

- [ ] 문제 시작 시 카운트다운 시작
- [ ] 답 선택 또는 입력 후 타이머 정지
- [ ] 시간 초과 시 자동 오답 처리
- [ ] 시간 초과 메시지 노출 확인
- [ ] 다음 문제에서 시간 초기화 확인
- [ ] 결과 화면 이동 시 interval 정리 확인

### 6.5 결과 화면 강화

- [ ] 정답/오답/정답률 표시 정확성 확인
- [ ] 최고 streak 표시 정확성 확인
- [ ] 타이머 모드 정보 표시 확인
- [ ] 오답 다시 풀기 버튼 가시성 확인

### 6.6 학습 기록 저장

- [ ] 결과 화면 진입 시 기록 저장 확인
- [ ] 같은 세션 중복 저장 방지 확인
- [ ] 새로고침 후 최근 기록 유지 확인
- [ ] 저장 실패 시 앱 동작 이상 없는지 확인

## 7. 완료 기준

- [x] 객관식과 직접 입력 두 가지 풀이 방식을 안정적으로 지원한다.
- [x] 타이머 모드가 문제 전환과 결과 화면까지 상태 꼬임 없이 동작한다.
- [x] 연속 정답 보상이 정확히 계산되고 표시된다.
- [x] 효과음 on/off가 일관되게 적용된다.
- [x] 결과 화면이 정보 전달력과 시각 완성도를 함께 개선한다.
- [x] 학습 기록이 localStorage에 저장되고 다시 읽힌다.
- [ ] 기존 v0.1 기능이 회귀 없이 유지된다.
